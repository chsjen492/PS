#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;


void find(vector<int> v) {
	int i;
	int sum = 0;
	int a, b;
	for (i = 0; i < 9; i++) {
		sum += v[i];
	}
	for (i = 0; i < 8; i++) {
		for (int j = i + 1; j < 9; j++) {
			if (sum - v[i] - v[j] == 100) {
				a = v[i];
				b = v[j];
			}
		}
	}
	remove(v.begin(), v.end(), a);
	remove(v.begin(), v.end(), b);
}
int main() {
	vector <int> height;
	int n;
	for (int i = 0; i < 9; i++) {
		cin >> n;
		height.push_back(n);
	}

	find(height);

	sort(height.begin(), height.end());
	for (int i = 0; i < 7; i++) {
		cout << height[i]<<endl;
	}
}
